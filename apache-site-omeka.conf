<VirtualHost *:28${LH_INSTANCE_NUM}0>
  DocumentRoot /var/www/html
  DirectoryIndex index.php

  # In this internal Apache config, we only want to log errors. The
  # external/main Apache can log accesses.
  ErrorLog /host/logs/error.log
  LogLevel warn
</VirtualHost>
